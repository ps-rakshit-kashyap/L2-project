# mcp_server/__init__.py
# Makes the mcp_server directory a Python package.
# This package contains the MCP (Model Context Protocol) server
# implementation that exposes resume analysis tools.
# The server runs as a subprocess, communicating via JSON-RPC over stdio.
